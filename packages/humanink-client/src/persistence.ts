export interface HumanInkUiState { readonly selectedContentId?: string; readonly query: string; readonly sidebarWidth: number; }
export const DEFAULT_HUMANINK_UI_STATE: HumanInkUiState = Object.freeze({ query: '', sidebarWidth: 320 });
export const HUMANINK_UI_STATE_KEY = 'humanink.ui-state';
export interface HumanInkStorageLike { getItem(key: string): string | null; setItem(key: string, value: string): void; removeItem(key: string): void; }
const MIN_WIDTH = 240; const MAX_WIDTH = 560;
function validState(value: unknown): HumanInkUiState { if (typeof value !== 'object' || value === null || Array.isArray(value)) return DEFAULT_HUMANINK_UI_STATE; const raw = value as Record<string, unknown>; if ((raw.selectedContentId !== undefined && typeof raw.selectedContentId !== 'string') || typeof raw.query !== 'string' || typeof raw.sidebarWidth !== 'number' || !Number.isFinite(raw.sidebarWidth) || raw.sidebarWidth < MIN_WIDTH || raw.sidebarWidth > MAX_WIDTH) return DEFAULT_HUMANINK_UI_STATE; return { ...(raw.selectedContentId === undefined ? {} : { selectedContentId: raw.selectedContentId }), query: raw.query, sidebarWidth: Math.round(raw.sidebarWidth) }; }
export function createHumanInkPersistence(storage: HumanInkStorageLike, key = HUMANINK_UI_STATE_KEY) { return { load(): HumanInkUiState { try { const raw = storage.getItem(key); return raw === null ? DEFAULT_HUMANINK_UI_STATE : validState(JSON.parse(raw)); } catch { return DEFAULT_HUMANINK_UI_STATE; } }, save(state: HumanInkUiState): void { const safe: HumanInkUiState = { ...(state.selectedContentId === undefined ? {} : { selectedContentId: state.selectedContentId }), query: state.query, sidebarWidth: Math.min(MAX_WIDTH, Math.max(MIN_WIDTH, Math.round(state.sidebarWidth))) }; storage.setItem(key, JSON.stringify(safe)); }, clear(): void { storage.removeItem(key); } }; }
