# AGENTS.md

本文件是 `HumanInk` 仓库中 AI 编码代理的协作规范，适用于仓库根目录及其子目录。

## 1. 工作前检查

- 修改任何文件前，先执行 `git status --short --branch`，确认当前分支、基线提交和是否存在用户未提交的改动。
- 不得覆盖、删除或重置用户已有的未提交改动；如发现与当前任务无关的改动，应保留并在结果中说明。
- 先阅读相关文件和现有测试，再选择最小、可回滚的修改范围。

## 2. Git 版本管理（每次改动必须留版本）

- 每个独立任务或逻辑改动完成后，必须创建 Git commit，不能只修改工作区而不留版本。
- 每个版本必须包含：代码/配置/文档改动、对应自测结果，以及必要时的 `CHANGELOG.md` 记录。
- 使用语义化版本号，版本号保存在仓库根目录的 `VERSION` 文件中：
  - `MAJOR`：不兼容的重大变更；
  - `MINOR`：向后兼容的新功能；
  - `PATCH`：向后兼容的修复、文档或内部改进。
- 每次提交需要按变更类型更新 `VERSION`；提交信息采用约定式格式，例如：
  - `feat: add ...`
  - `fix: correct ...`
  - `docs: update ...`
  - `chore: ...`
- 提交前必须检查 `git diff --check`，提交后必须用 `git status --short --branch` 确认工作区状态，并在交付说明中报告版本号和 commit hash。
- 未经用户明确要求，不执行破坏性操作（如 `git reset --hard`、强制推送、删除分支）或改写已有提交历史。

## 3. 写完必须自测

- 每次写入或修改完成后，必须运行与改动相关的测试或验证命令；不能以“改动很小”为理由跳过自测。
- 优先运行项目已有的最小相关测试，再根据时间和风险运行完整测试集。
- 如果项目尚未建立测试框架，至少执行：
  1. `git diff --check`；
  2. 配置/文档格式或语法检查；
  3. 对本次改动可执行的最小冒烟验证。
- 自测失败时不得宣称完成；应修复后重跑，或明确记录失败命令、错误原因和剩余风险。
- 最终说明必须列出实际执行的测试命令及结果，并区分“通过”“未执行”和“环境阻塞”。

## 4. 完成交付流程

按以下顺序完成任务：

1. 检查 Git 状态并阅读相关上下文；
2. 修改文件；
3. 运行自测并修复问题；
4. 查看 `git diff`，确认没有无关改动或敏感信息；
5. 更新 `VERSION` 与 `CHANGELOG.md`（如适用）；
6. 再次自测并提交 Git 版本；
7. 报告改动摘要、测试命令/结果、版本号和 commit hash。
